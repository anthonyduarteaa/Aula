// Conta de 1 a 30, mas pula os números múltiplos de 3

#include <stdio.h>

int main() {
    int i = 0;
    
    while(i <= 30){
        i++;
        if(i % 3 !=0){
            printf("%d\n", i);
        }
    }
    
    
    return 0;
}
