// Calcula a soma dos números de 1 a 50 usando while

#include <stdio.h>

int main() {
    int i = 1;
    int soma = 0;
    
    while (i <= 50) {
        soma = soma + i;
        i++;
    }
    
    printf("A soma dos números de 1 a 50 é: %d\n", soma);
    
    return 0;
}
